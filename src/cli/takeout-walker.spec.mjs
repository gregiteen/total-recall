import { describe, it, expect, vi } from 'vitest';
import * as m from './ingest/utils/takeout-walker.mjs';

describe('CLI Ingest: takeout-walker', () => {
  it('exports something', () => {
    expect(m).toBeDefined();
  });
});
