import { describe, it, expect, vi } from 'vitest';
import * as m from './ingest/utils/dedup.mjs';

describe('CLI Ingest: dedup', () => {
  it('exports something', () => {
    expect(m).toBeDefined();
  });
});
