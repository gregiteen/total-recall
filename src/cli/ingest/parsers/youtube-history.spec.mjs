import { describe, it, expect, vi } from 'vitest';
import * as m from './youtube-history.mjs';

describe('CLI Ingest: youtube-history', () => {
  it('exports something', () => {
    expect(m).toBeDefined();
  });
});
