# Memory

Use the Total Recall CLI for durable memory:

```bash
npx total-recall remember invariant "Never commit secrets." --importance 5
npx total-recall recall "secrets"
npx total-recall compile
```

Categories: invariants, preferences, facts, decisions, patterns, anti-patterns, concepts, lore.
