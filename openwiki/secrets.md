# Secrets & API keys

Store credentials in Total Recall's secrets store (`config/secrets.enc` / future CLI).

**Never** put API keys in openwiki pages or compiled IDE instruction files.

Planned CLI surface (TR_CORE_FOCUS Phase 4): `secret set|list|rotate|audit` and usage/cost meters.
