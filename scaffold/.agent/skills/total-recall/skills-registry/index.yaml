# Skills registry — populated at runtime by `total-recall skill` commands.
#
# This file ships as part of the install template, so it must start EMPTY.
# It previously shipped a snapshot of the maintainer's machine, which leaked
# local repository paths and private project names to every installer.
version: 1
skills: {}
