# Instruction Standards

Total Recall instruction management follows these rules:

1. **Version Control**: Every instruction template MUST have an explicit version number.
2. **Deprecation**: Never delete old instructions immediately. Mark them as `deprecated` to ensure active sessions don't break.
3. **Tone**: Direct, concise, and heavily geared towards code-level execution.
