---
type: query
title: "Total Recall — Dashboard"
tags: [query, dashboard]
---

# Total Recall Dashboard

> Auto-generated by `total-recall connect obsidian`. Powered by [Dataview](https://blacksmithgu.github.io/obsidian-dataview/).

## Active Memory Nodes

```dataview
TABLE title, category, confidence, importance, updated
FROM "Total Recall"
WHERE type = "memory" AND status = "active"
SORT importance DESC, updated DESC
LIMIT 20
```

## Recent Activity (Last 7 Days)

```dataview
TABLE title, category, updated
FROM "Total Recall"
WHERE type = "memory" AND date(updated) >= date(today) - dur(7 days)
SORT updated DESC
```

## Node Health

```dataview
TABLE length(rows) AS count, rows.category AS category
FROM "Total Recall"
WHERE type = "memory"
GROUP BY category
SORT count DESC
```
