# Total Recall Runtime Configuration
#
# CLI agent dispatch and embedding config lives in the meta-skill:
#   .agent/skills/total-recall/skills/cli-agents/agents.yml
#
# This file is kept for backward compatibility with the deploy system.
# The daemon reads agents.yml directly for all AI dispatch settings.
#
# Non-AI runtime settings (if any) can go here.

# Placeholder — agents.yml is the source of truth for all AI dispatch.
