---
endpoint: "https://api.deepseek.com/v1/chat/completions"
model: "deepseek-coder"
api_key_env: "DEEPSEEK_API_KEY"
temperature: 0.2
fallback_enabled: true
escalation_threshold: 3
---
# Frontier Intelligence Config
# This defines the OpenAI-compatible endpoint used when the local kernel
# (Gemma 4) fails a sandbox task repeatedly or requests a bypass.
