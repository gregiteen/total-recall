node_modules/
*.db
*.db-wal
*.db-shm
.DS_Store

# Secrets — NEVER commit
.env
.env.local
.env.*.local
*.key
*.pem

# Total Recall Workspace Projection
# The brain vault and queue must be tracked so the cloud agent can sync them.
.agent/memory-derived/
.agent/memory-inbox/
.agent/config/
.agent/config/
.agent/files/
.agent/logs/
.agent/.backups/
