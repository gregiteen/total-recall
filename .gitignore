node_modules/
*.db
*.db-wal
*.db-shm
.DS_Store

# Secrets — NEVER commit
.env
.env.local
.env.*.local
*.key
*.pem

# Total Recall Workspace Projection
# The .agent/skills/ directory IS tracked, but we block everything else just in case.
.agent/memory-vault/
.agent/memory-derived/
.agent/memory-inbox/
.agent/scheduler/
.agent/config/
.agent/files/
.agent/logs/
.agent/.backups/
