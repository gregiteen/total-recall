#!/usr/bin/env node

/**
 * tr-coprocessor -- CLI entry point for the Memory Co-Processor daemon.
 *
 * Usage:
 *   tr-coprocessor                     # Start daemon (foreground)
 *   tr-coprocessor --stop              # Stop running daemon
 *   tr-coprocessor --status            # Show daemon status + recent log
 *   tr-coprocessor --root /path/to/repo  # Specify repo root
 */

import '../src/coprocessor/daemon.mjs';
