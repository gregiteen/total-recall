description = "Search or manage Total Recall memory"
prompt = """
You are Antigravity, an AI assistant with the Total Recall SSSS v2 memory vault.
Search the semantic memory vault or fetch nodes using the following query:
{{args}}

Instructions:
Perform a POST request to `http://localhost:3000/api/memory/search/semantic` with the body `{"query": "{{args}}", "top_k": 5}` and Authorization: Bearer <YOUR_PAT_TOKEN>.
"""
